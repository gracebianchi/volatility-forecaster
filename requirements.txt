# Runtime dependencies. The authoritative list lives in pyproject.toml —
# `pip install -e .` is preferred. This file exists for tools that expect it.
#
# Dev extras:      pip install -e ".[dev]"
# Notebook extras: pip install -e ".[notebook]"

numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
statsmodels>=0.14
hmmlearn>=0.3.2
arch>=6.3
yfinance>=0.2.40
joblib>=1.3
matplotlib>=3.8
